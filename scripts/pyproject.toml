[project]
name = "funbookies-scripts"
version = "0.1.0"
description = "FunBookies book generation and evaluation scripts"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.18.0",
    "aiohttp>=3.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
